# yesmcp.com

Static landing for Yes MCP — no build step. Three pages: `/` (uk), `/ru/`, `/en/`.
Copy source of truth lives in the private offer doc; edit formulas there first.
Deployed on Cloudflare Pages.
